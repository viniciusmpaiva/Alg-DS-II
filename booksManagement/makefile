# Makefile para compilar e executar o código

# Comando de compilação
all: main.c management.c
	gcc -o main main.c management.c

# Comando de execução
run: all
	./main

# Limpeza de arquivos temporários
clean:
	rm -f main
